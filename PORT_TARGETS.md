# Port targets — `shlex-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `shlex` that codex-rs uses, and therefore the minimum subset that `shlex-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-137] (rank `58`, tier `direct`, status `To Do`)

**Cargo package name**: `shlex`
**Rust module name (in `use` statements)**: `shlex`

## Summary

- **2** distinct symbols imported by codex-rs
- **6** total `use` statements
- **5** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `shlex::try_join`

- **4 `use` site(s)** across **4 codex member crate(s)**: `codex-core`, `codex-execpolicy`, `codex-shell-command`, `codex-tui`
- Aliases used: `as shlex_try_join`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/exec_policy.rs:41` (`codex-core`) — `use shlex::try_join as shlex_try_join`
- `execpolicy/src/rule.rs:9` (`codex-execpolicy`) — `use shlex::try_join`
- `shell-command/src/parse_command.rs:7` (`codex-shell-command`) — `use shlex::try_join as shlex_try_join`
- `tui/src/exec_command.rs:6` (`codex-tui`) — `use shlex::try_join`

### `shlex::split`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-command`
- Aliases used: `as shlex_split`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-command/src/command_safety/windows_dangerous_commands.rs:5` (`codex-shell-command`) — `use shlex::split as shlex_split`
- `shell-command/src/parse_command.rs:6` (`codex-shell-command`) — `use shlex::split as shlex_split`
